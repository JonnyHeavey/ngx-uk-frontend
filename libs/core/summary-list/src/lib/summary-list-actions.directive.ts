import { Directive } from '@angular/core';

/**
 * Base directive for summary list actions components.
 * Contains common functionality shared between different design system implementations.
 */
@Directive()
export class SummaryListActionsDirective {
  // Currently no shared functionality, but provides base for extension
}
